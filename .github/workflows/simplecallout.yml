# set-secret-workflow.yml

name: Simple Call out

on:
  workflow_call:

jobs:
  job-hello-world:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2  
      - name: Step - Hello World
        id: set-credentials
        run: |
          echo "Hello world !!!"
        shell: bash
