name: This a only for my own test
run-name: ${{ github.actor }} is running a test workflow to try out reusable workflow

on:
  push:
    branches: 
        - feature/*
        - dev
  workflow_dispatch:
    branches: 
        - feature/*
        - dev

# permission can be added at job level or workflow level     
permissions: 
    id-token: write   # This is required for requesting the JWT 

jobs:
  build:
    runs-on: ubuntu-latest
    - uses: actions/checkout@v2
    - name: print environment
      run: | 
        echo "The current environment is ${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"

    - name: your action
      run: |
        echo "Now you can continue with your project!"
    
    - name: Call out "Simple call out workflow"
      uses: ilovedragon/capstone-project-playground/.github/workflows/test.yml@feature/workflow_callout-by-pohleng